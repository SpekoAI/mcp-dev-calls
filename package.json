{
  "name": "mcp-dev-calls",
  "private": true,
  "version": "0.1.0",
  "description": "agentvoice (@spekoai/mcp-calls) - an MCP (mcp/) that places real, disclosed phone calls, including owner call_me, via a backing server (server/) the MCP runs in-process by default, using SPEKO_API_KEY + @spekoai/sdk.",
  "type": "module",
  "license": "MIT",
  "author": "SpekoAI",
  "homepage": "https://github.com/SpekoAI/mcp-dev-calls",
  "repository": { "type": "git", "url": "git+https://github.com/SpekoAI/mcp-dev-calls.git" },
  "workspaces": ["server", "mcp"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces --if-present",
    "test:integration": "npm run build && node characterization/coverage-check.mjs && node characterization/run.mjs --target local && node scripts/mcp-e2e.mjs && node mcp/dist/index.js selftest",
    "dev:server": "npm run dev --workspace server",
    "dev:mcp": "npm run dev --workspace mcp",
    "start:server": "npm run start --workspace server",
    "start:mcp": "npm run start --workspace mcp"
  }
}
