{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "ai.speko/mcp-calls",
  "title": "Speko — AI Calls for Devs",
  "description": "Place real, disclosed outbound phone calls to businesses straight from your coding agent.",
  "repository": { "url": "https://github.com/SpekoAI/mcp-dev-calls", "source": "github" },
  "version": "0.3.2",
  "websiteUrl": "https://speko.ai",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@spekoai/mcp-calls",
      "version": "0.3.2",
      "transport": { "type": "stdio" },
      "environmentVariables": [
        {
          "name": "SPEKO_API_KEY",
          "description": "Your Speko API key (sk_...). Get one at https://platform.speko.dev. The MCP runs single-process and calls api.speko.dev directly. Easiest setup: `npx @spekoai/mcp-calls init`.",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "SPEKO_MCP_SERVER_URL",
          "description": "Optional: route through a hosted/remote Speko backing server instead of running in-process. When set, the API key lives on that server, not here.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        }
      ]
    }
  ]
}
