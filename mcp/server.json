{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "ai.speko/mcp-calls",
  "title": "agentvoice - AI calls for coding agents (by Speko)",
  "description": "Place real, disclosed outbound phone calls to businesses, or call your verified owner for remote control, straight from your coding agent.",
  "repository": {
    "url": "https://github.com/SpekoAI/mcp-dev-calls",
    "source": "github"
  },
  "version": "0.8.1",
  "websiteUrl": "https://agentvoice.to",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@spekoai/mcp-calls",
      "version": "0.8.1",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "SPEKO_API_KEY",
          "description": "Required in default single-process mode. Your Speko API key (sk_...); get one at https://platform.speko.dev. In remote mode this key belongs on the backing server instead.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "SPEKO_MCP_SERVER_URL",
          "description": "Optional: route through a hosted/remote Speko backing server instead of running in-process. When set, the API key lives on that server, not here.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "SPEKO_CLIENT_PROFILE",
          "description": "Client timeout profile written by init. In remote mode the MCP forwards only a known profile; unknown or missing values use poll-safe defaults.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "MCP_INTERNAL_KEY",
          "description": "Optional shared secret for remote mode. Set the same value on the MCP client and backing server; required when that server binds a non-loopback address.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "SPEKO_OWNER_PROFILE",
          "description": "Optional: portable call_me owner state for headless installs. Run `speko me verify` on a machine with a terminal, then `speko me export` and set the printed blob here. Credential-equivalent for ringing that owner number; store it like a key. An existing local owner state always wins.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "SPEKO_TOOLS",
          "description": "Optional: comma-separated allowlist of tools to register (e.g. call_me,get_call,check_call_readiness) to shrink the dialing surface on hosts that do not approval-gate tool calls. Unset or empty registers all 6 tools.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "SPEKO_TEST_MODE",
          "description": "Optional: set to 1 for a hermetic in-process simulation - all 6 tools run offline with no key, no network, and no real calls (results are marked [SIMULATED]). Refuses to start next to a live sk_* key or SPEKO_MCP_SERVER_URL. For CI and integration testing.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        }
      ]
    }
  ]
}
